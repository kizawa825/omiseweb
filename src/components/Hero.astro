---
const { content } = Astro.props;
const { pathname } = Astro.url;

// 現在のパスから言語コードを取得
const lang = pathname.split('/')[1];
const supportedLangs = ['en', 'th', 'zh'];
const currentLang = supportedLangs.find(l => l === lang);

// priceページのリンクを生成
const priceLink = currentLang ?
`/${currentLang}/price` : '/price';
---
{/*
  ヒーローセクションのデザインを大幅に刷新：
  - 従来の「背景画像 + テキストオーバーレイ」 は、コントラストや可読性（ユーザーファースト）の観点から課題がありました。
  - 2025年のUI/UXトレンドと信頼性を考慮し、「テキスト（左）」と「ビジュアル（右）」を明確に分けるスプリットレイアウトに変更します。
  - これにより、キャッチコピーが即座に目に入り、権威性のあるビジュアル（実績など）を右側に配置できます。
*/}
<section class="relative bg-white overflow-hidden">
  <div class="max-w-7xl mx-auto px-6">
    <div class="relative z-10 pb-16 pt-24 sm:pb-20 sm:pt-28 lg:pb-28 lg:pt-32 w-full">
      <div class="lg:grid lg:grid-cols-2 lg:gap-16 items-center">
        
        <div class="max-w-xl text-center lg:text-left">
          <h1 class="text-4xl font-bold tracking-tight text-neutral-900 sm:text-5xl md:text-6xl whitespace-pre-line">
            {content.catchphrase}
          </h1>
          <p class="mt-6 text-lg max-w-2xl mx-auto lg:mx-0 leading-8 text-neutral-600 whitespace-pre-line">
            {content.subcopy}
          </p>
          <div class="mt-10 flex items-center justify-center lg:justify-start gap-x-6">
            <a href="#contact" class="rounded-full bg-primary-600 px-6 py-3.5 text-base font-semibold text-white shadow-lg hover:bg-primary-700 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-primary-600 transition-all duration-300 ease-in-out transform hover:scale-105">
              {content.cta_main}
            </a>
            <a href={priceLink} class="text-base font-semibold leading-6 text-neutral-700 hover:text-neutral-900 transition-colors">{content.cta_sub} <span aria-hidden="true">→</span></a>
          </div>
          <div class="mt-12 flex flex-wrap justify-center lg:justify-start items-center gap-x-6 gap-y-3 text-sm text-neutral-500">
            {content.features.map(feature => (
              <span class="flex items-center">
                <svg class="w-5 h-5 mr-1.5 text-success-500" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
                  <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
                </svg>
                {feature}
              </span>
            ))}
          </div>
        </div>

        <div class="mt-12 lg:mt-0 lg:col-start-2">
          <div class="relative w-full h-80 lg:h-[500px] rounded-3xl shadow-xl overflow-hidden">
            {/* TODO: 
              ここに、権威性・信頼性のある高品質なビジュアルを配置してください。
              例：
              - 制作実績のウェブサイトが動作している様子のモンタージュ動画
              - プロフェッショナルなチームの写真
              - サービスの独自性を示す抽象的な3Dグラフィック
            */}
            <img 
              src="https://images.unsplash.com/photo-1551434678-e076c223a692?q=80&w=2070&auto=format&fit=crop" 
              alt="プロフェッショナルなWeb制作サービスのイメージ"
              class="w-full h-full object-cover"
            />
             <div class="absolute inset-0 bg-gradient-to-t from-black/10 to-transparent"></div>
          </div>
        </div>

      </div>
    </div>
  </div>
  
  {/* 装飾：右側に幾何学的なパターンを追加 */}
  <div class="absolute inset-y-0 right-0 hidden lg:block lg:w-1/2">
    <svg class="absolute top-0 right-0 transform translate-x-1/2 -translate-y-1/4 w-[64rem] h-[64rem] text-neutral-100/50" fill="currentColor" viewBox="0 0 100 100">
      <defs>
        <pattern id="geometric-pattern" patternUnits="userSpaceOnUse" width="40" height="40">
          <rect width="10" height="10" class="text-neutral-200/50" fill="currentColor"></rect>
          <rect x="20" y="20" width="10" height="10" class="text-neutral-200/50" fill="currentColor"></rect>
        </pattern>
      </defs>
      <rect fill="url(#geometric-pattern)" width="100" height="100"></rect>
    </svg>
  </div>
</section>
