---
// --- Props ---
const { content = {} } = Astro.props;
// --- Content Configuration ---
// Astro.propsから渡されるコンテンツ、またはデフォルト値を設定
const navLinks = content?.navLinks ??
[
  { href: '#features', name: '機能' },
  { href: '#pricing', name: '料金' },
  { href: '#faq', name: 'よくある質問' },
];
const cta = content?.cta ?? 'お問い合わせ';

// --- Language Configuration ---
// (中略: 言語設定ロジックは変更なし)
const { pathname } = Astro.url;
// ... (STEP 1〜4 は変更なし)
---

{/* ヘッダーコンポーネント
  - `bg-white/95`: 透明度を下げて信頼感をアップ
  - `backdrop-blur-lg`: ぼかしを強めて洗練された印象に
*/}
<header class="bg-white/95 backdrop-blur-lg sticky top-0 z-50 shadow-sm">
  <div class="max-w-7xl mx-auto px-6">
    <div class="flex justify-between items-center h-20">
      
      <div class="flex-shrink-0">
        <a href={logoHref} class="text-2xl font-extrabold text-primary-600 font-heading">Omiseweb</a>
      </div>
      
      <nav class="hidden md:flex items-center space-x-8">
       
 {navLinks.map(link => (
          <a href={link.href} class="font-medium text-gray-600 hover:text-primary-600 transition-colors">{link.name}</a>
        ))}
      </nav>

      <div class="flex items-center space-x-4">
        
        <div class="relative hidden md:block">
          <button id="lang-switcher-button" type="button" class="flex items-center space-x-2 text-sm text-gray-600 hover:text-primary-600 font-medium transition-colors" aria-haspopup="true" aria-expanded="false" aria-controls="lang-switcher-menu">
        
    <svg class="w-5 h-5" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" d="M12 21a9.004 9.004 0 008.716-6.747M12 21a9.004 9.004 0 01-8.716-6.747M12 21c2.485 0 4.5-4.03 4.5-9S14.485 3 12 3m0 18c-2.485 0-4.5-4.03-4.5-9S9.515 3 12 3m0 0a8.997 8.997 0 017.843 4.582M12 3a8.997 8.997 0 00-7.843 4.582m15.686 0A11.953 11.953 0 0112 10.5c-2.998 0-5.74-1.1-7.843-2.918m15.686 0A8.959 8.959 0 0121 12c0 .778-.099 1.533-.284 2.253m0 0A17.919 17.919 0 0112 16.5c-3.162 0-6.133-.815-8.716-2.247m0 0A9.015 9.015 0 013 
12c0-1.605.42-3.113 1.157-4.418" />
            </svg>
            <span>{currentLanguage.name}</span>
            <svg id="lang-switcher-chevron" class="w-4 h-4 transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
            </svg>
      
    </button>
          
          <div id="lang-switcher-menu" class="absolute right-0 mt-2 w-32 bg-white rounded-md shadow-lg py-1 z-10 hidden ring-1 ring-black ring-opacity-5" role="menu">
            {sortedLanguages.map(lang => (
              <a 
                href={lang.href} 
  
              role="menuitem"
                class:list={[
                  "block px-4 py-2 text-sm text-gray-700 hover:bg-primary-50",
                  // 現在選択中の言語をハイライト表示
                  { "bg-primary-50 font-semibold text-primary-600": lang.code === currentLangCode }
   
             ]}
              >
                {lang.name}
              </a>
            ))}
          </div>
        </div>
        
     
   <a href="#contact" class="hidden sm:inline-block bg-primary-600 text-white font-semibold px-5 py-2 rounded-full hover:bg-primary-700 transition-all duration-300 ease-in-out transform hover:scale-105">
          {cta}
        </a>

        <button id="mobile-menu-button" class="md:hidden p-2 rounded-md text-gray-600 hover:bg-gray-100" aria-label="Toggle menu" aria-controls="mobile-menu" aria-expanded="false">
            <svg class="w-6 h-6" id="mobile-menu-open-icon" 
xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" />
            </svg>
             <svg class="w-6 h-6 hidden" id="mobile-menu-close-icon" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
           
 </svg>
        </button>
      </div>
    </div>
  </div>

  <div id="mobile-menu" class="hidden md:hidden bg-white border-t border-gray-200">
    <nav class="flex flex-col p-4">
      <div class="space-y-2">
        {navLinks.map(link => (
          <a href={link.href} class="block px-4 py-2 rounded-md text-gray-600 hover:bg-gray-100 hover:text-primary-600">{link.name}</a>
        ))}
        <a href="#contact" class="block px-4 py-2 rounded-md bg-primary-600 text-white 
text-center font-semibold">{cta}</a>
      </div>
      
      <div class="border-t border-gray-200 pt-4 mt-4">
          <p class="px-4 pb-2 text-sm font-semibold text-gray-500">言語を選択</p>
          <div class="space-y-1">
             {sortedLanguages.map(lang => (
              <a 
             
   href={lang.href} 
                class:list={[
                  "block px-4 py-2 rounded-md text-gray-600 hover:bg-gray-100 hover:text-primary-600",
                  { "bg-gray-100 font-semibold text-primary-600": lang.code === currentLangCode }
                ]}
              >
 
               {lang.name}
              </a>
            ))}
          </div>
      </div>
    </nav>
  </div>
</header>

<script>
// ... (中略)
</script>
