---
const { content } = Astro.props;
---
{/*
  ブラッシュアップ：
  - セクション背景を `bg-white` に変更。
  - カラーを `gray` -> `neutral` に統一。
  - <details> タグ の構造はアクセシビリティに優れモダンなため維持。
  - ホバーエフェクト を `hover:shadow-strong` に変更し、開いた時（`open:`）は `shadow-strong` を適用。
*/}
<section class="py-24 sm:py-32 bg-white">
  <div class="max-w-4xl mx-auto px-6">
    <div class="text-center max-w-2xl mx-auto">
        <p class="text-base font-semibold leading-7 text-primary-600">{content.eyebrow}</p>
        <h2 class="text-3xl font-bold tracking-tight text-neutral-900 sm:text-4xl">{content.title}</h2>
        <p class="mt-4 text-lg leading-8 text-neutral-600">{content.subtitle}</p>
    </div>
    <div class="mt-12 space-y-4">
      {content.faqs.map(faq => (
        <details class="group rounded-xl bg-white p-6 border border-neutral-200 shadow-subtle transition-all duration-300 ease-in-out hover:shadow-strong open:shadow-strong [&_summary::-webkit-details-marker]:hidden">
          <summary class="flex cursor-pointer items-center justify-between gap-1.5 text-neutral-900">
            <h2 class="font-semibold text-lg">{faq.q}</h2>
            <span class="relative h-5 w-5 shrink-0 transition-transform duration-300 ease-in-out">
              <svg xmlns="http://www.w3.org/2000/svg" class="absolute inset-0 h-5 w-5 opacity-100 transition-all duration-300 ease-in-out group-open:opacity-0 group-open:rotate-90" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                <path stroke-linecap="round" stroke-linejoin="round" d="M12 9v6m3-3H9" />
              </svg>
              <svg xmlns="http://www.w3.org/2000/svg" class="absolute inset-0 h-5 w-5 opacity-0 transition-all duration-300 ease-in-out group-open:opacity-100 group-open:rotate-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                <path stroke-linecap="round" stroke-linejoin="round" d="M15 12H9" />
              </svg>
            </span>
          </summary>
          <div class="mt-4 leading-relaxed text-neutral-600">
            {/* set:html={faq.a} など、マークダウンを許可する場合はこちらを使う */}
            <p>{faq.a}</p>
          </div>
        </details>
      ))}
    </div>
 </div>
</section>
