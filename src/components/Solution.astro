---
const { content } = Astro.props;
---

<section class="relative overflow-hidden bg-neutral-950 py-24 sm:py-28 lg:py-32 text-base-100" data-dark-section>
  <div class="pointer-events-none absolute inset-0">
    <div class="absolute -top-32 right-[15%] h-80 w-80 rounded-full bg-primary/30 blur-[150px]"></div>
    <div class="absolute bottom-0 left-[10%] h-[420px] w-[420px] rounded-full bg-secondary/30 blur-[180px]"></div>
    <div class="absolute inset-x-0 bottom-0 h-48 bg-gradient-to-t from-neutral-950 via-transparent to-transparent"></div>
    <div class="absolute inset-0 bg-[linear-gradient(150deg,_rgba(255,255,255,0.06)_0%,_rgba(255,255,255,0)_60%)]"></div>
  </div>

  <div class="relative mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <div class="text-center">
      <div class="inline-flex items-center gap-2 rounded-full border border-white/15 bg-white/10 px-4 py-2 text-xs font-semibold uppercase tracking-[0.36em] text-primary-200">
        Signature Strengths
      </div>
      <h2 class="mt-8 text-3xl font-bold tracking-tight text-white sm:text-4xl lg:text-5xl">
        {content.title}
      </h2>
      <p class="mt-4 text-lg leading-relaxed text-white/70">
        {content.subtitle}
      </p>
    </div>

    <div class="mt-18 space-y-10 lg:mt-20">
      {content.solutions.map((solution, index) => (
        <article
          class:list={[
            'group relative overflow-hidden rounded-[2.5rem] border border-white/10 bg-white/5 p-8 shadow-strong backdrop-blur-xl transition duration-500 lg:flex lg:items-stretch lg:gap-12 lg:p-12',
            { 'lg:flex-row-reverse': index % 2 === 1 },
          ]}
        >
          <div class="absolute -left-10 top-0 hidden h-full w-20 bg-gradient-to-b from-primary/40 to-transparent blur-3xl lg:block"></div>
          <div class="absolute -right-10 bottom-0 hidden h-32 w-24 bg-gradient-to-t from-secondary/40 to-transparent blur-2xl lg:block"></div>

          <div class="relative flex-1 space-y-6">
            <div class="inline-flex items-center gap-2 rounded-full border border-primary/30 bg-primary/10 px-4 py-2 text-xs font-semibold uppercase tracking-[0.32em] text-primary-200">
              Strength {String(index + 1).padStart(2, '0')}
            </div>
            <div class="space-y-3">
              <p class="text-sm font-medium uppercase tracking-[0.38em] text-primary-100">
                {solution.subtitle}
              </p>
              <h3 class="text-2xl font-semibold text-white sm:text-3xl">
                {solution.title}
              </h3>
            </div>
            <p class="text-base leading-relaxed text-white/70 lg:text-lg">
              {solution.description}
            </p>

            <div class="flex flex-wrap items-center gap-3 pt-4 text-xs uppercase tracking-[0.32em] text-white/30">
              <span class="inline-flex h-1 w-8 rounded-full bg-gradient-to-r from-primary via-secondary to-accent"></span>
              Tailored Strategy · Culture-Driven Design · Growth Architecture
            </div>
          </div>

          <figure class="relative mt-8 flex flex-1 items-center justify-center lg:mt-0">
            <div class="relative w-full overflow-hidden rounded-[2rem] border border-white/15 bg-white/5 shadow-subtle">
              <img
                src={solution.image}
                alt={solution.title}
                class="h-full w-full object-cover transition duration-700 group-hover:scale-105"
              />
              <div class="absolute inset-0 bg-gradient-to-t from-neutral-950/60 via-transparent to-transparent"></div>
              <div class="absolute top-6 left-6 rounded-2xl border border-white/20 bg-white/10 px-4 py-3 text-xs font-semibold uppercase tracking-[0.28em] text-white/70 backdrop-blur">
                Omiseweb Blueprint
              </div>
              <div class="absolute bottom-6 left-6 right-6 flex items-center justify-between rounded-2xl border border-white/15 bg-white/10 px-4 py-3 text-sm text-white/80 backdrop-blur">
                <span>Impact Scenario</span>
                <span class="font-semibold text-primary-200">+187% engagement</span>
              </div>
            </div>
          </figure>
        </article>
      ))}
    </div>

    <div class="mt-20 flex flex-col items-center gap-4 text-center">
      <p class="text-sm font-semibold uppercase tracking-[0.38em] text-white/40">
        Unified Experience · Conversion Strategy · Continuous Growth
      </p>
      <div class="inline-flex items-center gap-3 rounded-2xl border border-white/15 bg-white/10 px-8 py-4 text-sm font-semibold uppercase tracking-[0.28em] text-white/70 shadow-subtle backdrop-blur">
        Our strength is in orchestrating every detail end-to-end
      </div>
    </div>
  </div>
</section>
