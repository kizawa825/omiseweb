---
const { content } = Astro.props;
---
{/*
  ブラッシュアップ：
  - セクション背景を `bg-gray-50` -> `bg-white` に変更 (Problemセクションと交互)。
  - カードのホバーエフェクトを `hover:-translate-y-2 hover:shadow-xl` から、より滑らかな `transform hover:scale-[1.02] hover:shadow-strong` に変更。
  - 影を `shadow-lg` から `shadow-xl` に変更し、より存在感を出す。
*/}
<section class="py-24 sm:py-32 bg-white">
  <div class="max-w-7xl mx-auto px-6">
    <div class="text-center max-w-2xl mx-auto">
      <p class="text-base font-semibold leading-7 text-primary-600">{content.eyebrow}</p>
      <h2 class="text-3xl font-bold tracking-tight text-neutral-900 sm:text-4xl">{content.title}</h2>
      <p class="mt-4 text-lg leading-8 text-neutral-600">{content.subtitle}</p>
    </div>

    <div class="swiper solution-swiper mt-16">
      <div class="swiper-wrapper py-4">
        {content.solutions.map(solution => (
          <div class="swiper-slide h-full">
            <div class="bg-white rounded-xl p-8 h-full shadow-xl border border-neutral-100 transition-all duration-300 ease-in-out transform hover:scale-[1.02] hover:shadow-strong">
              {/* TODO: プレースホルダー画像を差し替えてください */}
              <img src={solution.image} alt={solution.title} class="w-full h-48 object-cover rounded-lg mb-6 shadow-md" />
              <h3 class="text-lg font-semibold leading-7 text-primary-600">{solution.subtitle}</h3>
              <p class="mt-2 text-xl font-bold tracking-tight text-neutral-900">{solution.title}</p>
              <p class="mt-4 text-base leading-7 text-neutral-600">{solution.description}</p>
            </div>
          </div>
        ))}
      </div>
      <div class="swiper-pagination solution-pagination"></div>
    </div>
  </div>
</section>
