---
const { content } = Astro.props;
const { pathname } = Astro.url;

// 現在のパスから言語コードを取得
const lang = pathname.split('/')[1];
const supportedLangs = ['en', 'th', 'zh'];
const currentLang = supportedLangs.find(l => l === lang);

// worksページのリンクを生成
const worksLink = currentLang ?
`/${currentLang}/works` : '/works';
---
{/*
  ブラッシュアップ：
  - セクション背景を `bg-slate-50` -> `bg-neutral-50` に変更。
  - テキストカラーを `slate` -> `neutral` に変更。
  - ホバーエフェクト（group-hover） はモダンで効果的なため維持。
*/}
<section id="works" class="py-20 sm:py-24 bg-neutral-50">
  <div class="max-w-7xl mx-auto px-6">
    <div class="text-center max-w-2xl mx-auto">
        <p class="text-base font-semibold leading-7 text-primary-600">{content.eyebrow}</p>
        <h2 class="text-3xl font-bold tracking-tight text-neutral-900 sm:text-4xl">{content.title}</h2>
        <p class="mt-4 text-lg leading-8 text-neutral-600">{content.subtitle}</p>
    </div>

    <div class="swiper works-swiper mt-12">
        <div class="swiper-wrapper py-4">
            {content.cases.map(work => (
                <div class="swiper-slide group">
                 <div class="relative overflow-hidden rounded-xl h-full shadow-lg border border-neutral-100">
                        {/* TODO: プレースホルダー画像を差し替えてください */}
                        <img src={work.image} alt={work.name} class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-105" style="min-height: 400px;" />
                        <div class="absolute inset-0 bg-gradient-to-t from-black/80 to-transparent transition-opacity duration-300 group-hover:from-black/90"></div>
                        <div class="absolute bottom-0 left-0 p-6 w-full">
                            <h3 class="text-xl font-bold text-white">{work.name}</h3>
                            <div class="mt-4 max-h-0 opacity-0 group-hover:max-h-40 group-hover:opacity-100 transition-all duration-500 overflow-hidden">
                                <p class="text-sm text-neutral-300"><strong class="font-semibold text-amber-400">Before:</strong> {work.before}</p>
                                <p class="mt-2 text-sm text-neutral-200"><strong class="font-semibold text-green-400">After:</strong> {work.after}</p>
                            </div>
                        </div>
                    </div>
                </div>
            ))}
        </div>
        <div class="swiper-pagination works-pagination"></div>
    </div>

    <div class="mt-12 text-center">
      <a href={worksLink} class="rounded-full bg-primary-600 px-6 py-3 text-sm font-semibold text-white shadow-lg hover:bg-primary-700 transition-all duration-300 ease-in-out transform hover:scale-105">
        {content.buttonText}
      </a>
    </div>
  </div>
</section>
