---
// デフォルトのコンテンツを定義
const defaultContent = {
  title: "さあ、あなたのビジネスの本当の価値を、\nWebサイトで日本のファンに届けませんか？",
  subtitle: "私たちが、言葉や文化、デジタルの壁を越えるお手伝いをします。",
  buttonText: "今すぐ無料で相談してみる",
  notice: "お問い合わせ後、24時間以内にご希望の言語でご連絡します。無理な営業は一切いたしません。"
};
// contentが渡されない場合は、defaultContentを使用するように修正
const { id, content = defaultContent } = Astro.props;
---
{/*
  ブラッシュアップ：
  - このコンポーネントのデザイン は既に強力で、テーマ（グラデーション、反転ボタン）にも合致しているため、大きな変更は不要。
  - 角丸を `rounded-md` -> `rounded-full` に変更し、ボタンのアイキャッチを強化。
  - 影 `shadow-lg` とホバーエフェクト `hover:scale-105` を追加。
*/}
<section id={id} class="bg-gradient-to-r from-primary-700 to-primary-800">
  <div class="max-w-4xl mx-auto text-center py-20 px-6 sm:py-24">
    <h2 class="text-3xl font-bold tracking-tight text-white sm:text-4xl whitespace-pre-line">{content.title}</h2>
    <p class="mt-4 text-lg leading-8 text-primary-200 max-w-2xl mx-auto">{content.subtitle}</p>
    <a href="#contact" class="mt-10 inline-flex w-full items-center justify-center rounded-full border border-transparent bg-white px-8 py-3.5 text-base font-semibold text-primary-700 shadow-lg hover:bg-neutral-100 sm:w-auto transition-all duration-300 ease-in-out transform hover:scale-105">
      {content.buttonText}
    </a>
    <p class="mt-6 text-sm text-primary-200">{content.notice}</p>
  </div>
</section>
