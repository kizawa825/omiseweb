---
const defaultContent = {
  description: "日本のファンに、あなたのお店を届ける。",
  navigation: "ナビゲーション",
  navLinks: [
    { name: "トップ", href: "/" },
    { name: "私たちについて", href: "/about" },
    { name: "制作事例", href: "/works" },
    { name: "料金プラン", href: "/price" },
    { name: "ブログ", href: "/blog" }
  ],
  supportedLanguages: "対応言語",
  languages: ["日本語", "English", "中文", "ภาษาไทย"],
  contact: "お問い合わせ",
  contactButton: "無料相談はこちら",
  copyright: "© 2025 Omiseweb. All rights reserved."
};

const { content = defaultContent } = Astro.props;
const navLinks = Array.isArray(content?.navLinks) ? content.navLinks : [];
const languages = Array.isArray(content?.languages) ? content.languages : [];
const currentYear = new Date().getFullYear();
---
{/*
  ブラッシュアップ：
  - カラーパレットを `gray` -> `neutral` に変更。
  - `bg-gray-950` -> `bg-neutral-950`
  - `text-gray-400` -> `text-neutral-400`
  - `border-gray-800` -> `border-neutral-800`
*/}
<footer class="bg-neutral-950 text-neutral-400">
  <div class="max-w-7xl mx-auto px-6 py-16">
    <div class="grid grid-cols-1 md:grid-cols-4 gap-10">
      <div class="md:col-span-1">
        <h3 class="text-2xl font-bold text-white font-heading">Omiseweb</h3>
        <p class="mt-3 text-sm leading-6">{content.description}</p>
      </div>
      <div class="md:col-start-3">
        <h4 class="font-semibold text-neutral-200">{content.navigation}</h4>
        <ul class="mt-4 space-y-3 text-sm">
          {navLinks.map((item) => (
            <li><a href={item.href} class="hover:text-white transition-colors duration-300">{item.name}</a></li>
          ))}
        </ul>
      </div>
      <div>
        <h4 class="font-semibold text-neutral-200">{content.supportedLanguages}</h4>
        <ul class="mt-4 space-y-3 text-sm">
          {languages.map((lang) => (<li>{lang}</li>))}
        </ul>
      </div>
    </div>
    <div class="mt-16 border-t border-neutral-800 pt-8 flex flex-col sm:flex-row justify-between items-center text-sm">
      <p>&copy; {currentYear} Omiseweb. All rights reserved.</p>
       <a href="#contact" class="mt-6 sm:mt-0 inline-block bg-primary-600 text-white font-medium px-6 py-2.5 rounded-full hover:bg-primary-500 transition-all duration-300 ease-in-out transform hover:scale-105">
          {content.contactButton}
        </a>
    </div>
  </div>
</footer>
